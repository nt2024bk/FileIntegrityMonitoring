## integrity_check.sh
**Description**: Monitors file integrity by comparing current file hashes with a saved baseline using `sha256sum`. Generates a report of any changes.

**Usage**:
```bash
chmod +x integrity_check.sh
sudo ./integrity_check.sh
